import { Box, makeStyles, Theme} from "@material-ui/core";
import { useEffect, useState } from "react";

interface Iprops {
 
}

function QrGenerator(props: Iprops) {
  return (
   <Box>
       TEST
   </Box>
  );
}

const useStyles = makeStyles((theme: Theme) => ({}));


export default QrGenerator; 